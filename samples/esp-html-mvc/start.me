/*
    start.me - MakeMe build file (esp-html-mvc)
 */

Me.load({
    targets: {
        comp: {
            build: `run('esp -r -q compile')`,
        },
        run: {
            depends: ['comp'],
            run: 'appweb -v',
        },
        cleanCache: {
            type: 'clean',
            action: "rm('cache/*')",
        },
    },
})
