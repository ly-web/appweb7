{
    mode: "debug",
    modes: {
        debug: {
            server: {
                showErrors: true,
                username: "admin",
                update: true,
                keepSource: true,
            },
        },
        release: {
            server: {
                showErrors: false,
                update: false,
                keepSource: false,
            },
        },
    },
    settings: {
        components: ["server"],
        redirect: "secure",
        timeouts: {
            session: 1800,
        },
    },
    server: {
        authStore: "app",
        database: "${DATABASE}://${NAME}.${DATABASE}",
        jsonResponses: 1,
        xsrfToken: true,
    },
}
