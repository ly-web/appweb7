/*
    start.bit - esp-html-mvc build file
 */

Bit.load({
    targets: {
        comp: {
            build: `
                strace('Run', 'recess -compile templates/esp-html-mvc/all.less')
                let result = Cmd.run('recess -compile templates/esp-html-mvc/all.less')
                Path('templates/esp-html-mvc/all.css').write(result)
            `,
        },
    },
})
