/*
    libappweb.bit -- Bit File for the Appweb library
 */

Bit.load({

    targets: {
        libappweb: {
            type: 'lib',
            headers: [ '*.h' ],
            sources: [ '*.c' ],
            depends: [ 'libhttp' ],
        },
	},
})
