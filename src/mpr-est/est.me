/*
    est.me -- EST Component
 */

Me.load({ 
    targets: {
        est: {
            configurable: true,
            conflicts:    [ 'openssl', 'matrixssl', 'nanossl' ]
            depends:      [ 'libestssl' ],
            ifdef:        [ 'ssl' ],
        },
        libestssl: {
            type:         'lib',
            static:       true,
            sources:      [ '*.c' ],
            depends:      [ 'libest' ],
            ifdef:        [ 'est' ],
        }
    }
})
