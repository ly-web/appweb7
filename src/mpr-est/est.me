/*
    est.me -- EST Component
 */

Me.load({ 
    targets: {
        est: {
            libname:      'libmpr-estssl',
            configurable: true,
            conflicts:    [ 'openssl', 'matrixssl', 'mbedtls', 'nanossl' ]
            ifdef:        [ 'ssl' ],
            type:         'lib',
            static:       true,
            sources:      [ '*.c' ],
            depends:      [ 'libest' ],
            location:     '${SRC}/mpr-est',
        }
    }
})
