/*
    start.me - MakeMe file (esp-html-mvc)
 */

Me.load({
    targets: {
        'build-html-mvc': {
            build: `
                trace('Compile', 'esp compile')
                run('esp -q compile')
            `,
        },
        run: {
            depends: ['comp'],
            run: 'esp -v',
        },
        cleanCache: {
            type: 'clean',
            action: `rm('cache/*')`,
        },
    },
})
